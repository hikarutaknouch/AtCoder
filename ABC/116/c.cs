using System;

class Program
{
  /// <summary>
  ///   The main entry point for the application
  /// </summary>
  [STAThread]
  public static void Main(string[] args)
  {
    int n = int.Parse(Console.ReadLine());
    int[] h = Console.ReadLine().Split(' ').Select(t => int.Parse(t)).ToArray();
    int[] h1 = new int[n];
    for (int i = 0; i < n; i ++) h1[i] = 0;
    while (true)
    {
      if ()
    }
  }
}
