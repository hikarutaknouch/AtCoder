using System;
using System.Linq;

class Program
{
    static void Main(string[] args) {
        int[] s = Console.ReadLine().Split().Select(int.Parse).ToArray();
        int n = s[0];
        int m = s[1];
        int[][] list = new int[n][];
        for (int i = 0; i < m; i ++) {
            int[] s = Console.ReadLine().Split().Select(int.Parse).ToArray();
        }
    }
}